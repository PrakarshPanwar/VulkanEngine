#pragma once

struct VertexOutput
{
	vec3 WorldPosition;
	vec3 ViewPosition;
	vec3 Normal;
	mat3 WorldNormals;
	vec2 TexCoord;
};

struct TessellationOutput
{
	vec3 WorldPosition;
	vec3 ViewPosition;
	mat4 Transform;
	mat3 WorldNormals;
	vec2 TexCoord;
};
